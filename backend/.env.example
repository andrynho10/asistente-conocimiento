# Database Configuration
# Para desarrollo local, usar SQLite. Para producción, cambiar a PostgreSQL.
DATABASE_URL=sqlite:///./asistente_conocimiento.db

# Security Configuration
# IMPORTANTE: Generar SECRET_KEY con: python -c "import secrets; print(secrets.token_hex(32))"
# Mínimo 32 bytes (64 caracteres hexadecimales)
SECRET_KEY=your-secret-key-here-replace-with-secure-random-value-min-64-chars

# JWT Configuration
JWT_EXPIRATION_HOURS=24
JWT_ALGORITHM=HS256

# Ollama / LLM Configuration
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=llama3.1:8b-instruct-q4_K_M

# Development Settings
DEBUG=true
LOG_LEVEL=info

# CORS Settings (separar con comas para múltiples orígenes)
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
